<% content_for :title, "User page" %>

<h1 class="text-center mt-4"><%= @user.username %>'s profile</h1>

<%= gravatar_for(@user, size: 200) %>

<h3 class="text-center mt-4">Products</h3>

<%= render "products/product" %>